---
description: Squash-merge a worktree branch's PR and clean up.
---

Use the wt-merge skill. If invoked without arguments, auto-detect the target worktree. Otherwise merge the worktree the user specified.
