{
  "hooks": [
    {
      "event": "plugin:installed",
      "action": {
        "type": "command",
        "command": "python ${PLUGIN_DIR}/scripts/setup_react.py ${PROJECT_DIR}"
      },
      "description": "Initialize React project structure when plugin is installed"
    }
  ]
}
